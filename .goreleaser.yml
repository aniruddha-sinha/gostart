version: 1
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    binary: gostart-$(uname -s | tr 'A-Z' 'a-z')-$(uname -m)

# Automatically generates a clean changelog based on your git commits
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'